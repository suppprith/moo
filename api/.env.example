# Copy to api/.env and fill in. The LLM stages (query expansion, claim
# extraction, evidence linking) use Gemini; without a key they fall back to
# deterministic heuristics. .env is gitignored.

GEMINI_API_KEY=your-key-here

# Optional overrides:
# MOO_LLM_MODEL=gemini-2.5-flash
